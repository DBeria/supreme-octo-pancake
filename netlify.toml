[build]
  base = "client/"
  command = "npm run build"
  publish = "dist"

  [[redirects]]
  from = "/api/*"
  to = "https://pocus-world-backend.onrender.com/api/:splat"
  status = 200
  force = true
